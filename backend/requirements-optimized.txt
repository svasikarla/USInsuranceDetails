# Optimized Requirements for Serverless Deployment
# This file removes unnecessary dependencies and makes OCR optional

# Core Framework
fastapi==0.111.0
uvicorn==0.22.0
pydantic==2.8.2
pydantic-settings==2.2.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator>=2.0.0

# Database
sqlalchemy==2.0.41
psycopg[binary]

# Supabase Integration
supabase==1.0.3

# File Upload/Processing
python-multipart==0.0.6
pypdf2==3.0.1

# AI/ML (Primary - Google Gemini)
google-generativeai==0.8.3

# Testing
pytest==7.3.1
httpx==0.24.1

# REMOVED DEPENDENCIES (Not used in codebase):
# - spacy==3.5.3 (40-500 MB, never imported) ❌
# - celery==5.2.7 (not used, threading used instead) ❌
# - redis==4.5.5 (not used, was for celery) ❌
# - pytesseract==0.3.10 (requires system Tesseract, optional) ⚠️

# OPTIONAL: Uncomment if you need OCR functionality
# NOTE: These require system-level dependencies (Tesseract)
# pytesseract==0.3.10
# pdf2image==1.16.3
# Pillow==10.0.0

# OPTIONAL: Uncomment for additional AI providers (fallback)
# openai>=1.0.0
# anthropic>=0.3.0
